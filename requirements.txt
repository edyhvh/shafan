# OCR and Computer Vision Libraries
paddlepaddle>=3.2.2 # Uncomment if needed - large download
paddleocr>=3.3.2     # Uncomment if needed - requires paddlepaddle
pdf2image>=1.17.0
# opencv-python>=4.8.0  # Removed to avoid conflict with opencv-contrib-python
opencv-contrib-python>=4.8.0

# Image processing and PDF handling
Pillow>=10.0.0
pytesseract>=0.3.10  # Alternative OCR engine
poppler-utils>=0.1.0  # Required for pdf2image on some systems

# Data processing and utilities
numpy>=1.24.0
pandas>=2.0.0

# Text processing (json and re are built-in, but adding utilities)
regex>=2023.0.0  # Enhanced regex functionality

# Development and testing
pytest>=7.0.0
pytest-cov>=4.0.0  # Test coverage reporting
ruff>=0.1.0  # Linter and formatter
black>=23.0.0  # Code formatter

# HTTP and file handling
requests>=2.31.0
python-dotenv>=1.0.0  # For loading .env files securely
PyYAML>=6.0.0  # YAML configuration file support

# HTML parsing and processing
beautifulsoup4>=4.12.0  # HTML parsing for Delitzsch extraction
lxml>=4.9.0  # Fast XML/HTML parser backend

# Google Gemini API
# google-genai>=0.2.0  # Gemini API client (new package)
# google-generativeai>=0.3.0  # Deprecated, use google-genai instead
# won't use it since claude proved to be better so far

# Anthropic Claude API
anthropic>=0.39.0  # Claude API client for vision tasks

# xAI Grok API
openai>=1.0.0  # Grok API client (compatible interface)
httpx>=0.25.0  # HTTP client for API calls

# JSON schema validation
jsonschema>=4.0.0  # JSON schema validation

# Logging and utilities
python-dateutil>=2.8.0
tqdm>=4.65.0  # Progress bars

# Optional: For advanced image processing
scikit-image>=0.21.0
matplotlib>=3.7.0  # For visualization/debugging

# Caching and performance
diskcache>=5.6.0  # High-performance disk-based caching

# AI libraries for nakdimon
wandb>=0.17.0
prettytable>=3.6.0
torch>=2.0.0